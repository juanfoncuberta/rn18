import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1, 
 
    },
    cellView: {
        height:120,
        borderWidth:1,
        borderColor: 'blue', 
        alignItems: 'center',
        justifyContent: 'center'
    }
})